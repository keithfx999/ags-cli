# AGR CLI Configuration File
# Copy to ~/.agr/config.toml and fill in credentials.

# output = "text"                              # Optional default for non-streaming commands (text or json)
# region = "ap-guangzhou"                      # Optional (default: ap-guangzhou)
# domain = "tencentags.com"                    # Optional (default: tencentags.com)
# cloud_endpoint = "ags.tencentcloudapi.com"   # Optional (default: ags.tencentcloudapi.com)

[auth]
secret_id = ""    # Required
secret_key = ""   # Required
# token = ""       # Optional STS session token; prefer TENCENTCLOUD_TOKEN in CI/CD

# [sandbox]
# default_user = "user"   # Optional (default: user)
