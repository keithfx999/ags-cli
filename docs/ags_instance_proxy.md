## ags instance proxy

Forward an instance port to localhost

### Synopsis

Forward a remote instance port to a local address, similar to kubectl port-forward.

Port Syntax:
  <remote_port>                Forward remote port to the same local port
  <local_port>:<remote_port>   Forward remote port to a specific local port

Examples:
  ags instance proxy <id> 8080
  ags instance proxy <id> 3000:8080
  ags instance proxy <id> 3000:8080 --address 0.0.0.0

```
ags instance proxy <instance-id> [local_port:]<remote_port> [flags]
```

### Options

```
      --address string   Local address to bind to (default "127.0.0.1")
  -h, --help             help for proxy
      --verbose          Enable verbose request logging
```

### Options inherited from parent commands

```
      --api-key string      API key
      --backend string      API backend: e2b or cloud
      --config string       config file (default is $HOME/.ags/config.toml)
      --domain string       Base domain (default: tencentags.com)
      --internal            Use internal endpoints
      --jq string           jq expression (only with -o json)
      --no-color            Disable ANSI color output
      --non-interactive     Disable interactive behaviors
  -o, --output string       output format: text, json, or ndjson
      --region string       Region for API access (default: ap-guangzhou)
      --secret-id string    Tencent Cloud SecretID
      --secret-key string   Tencent Cloud SecretKey
```

### SEE ALSO

* [ags instance](ags_instance.md)	 - Manage sandbox instances

###### Auto generated by spf13/cobra on 14-May-2026
