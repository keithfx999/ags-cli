## ags instance create

Create a new instance

### Synopsis

Create a new sandbox instance from a tool template.

Use --tool-name/-t for tool name (e2b/cloud backend) or --tool-id for tool ID (cloud backend only).

Mount option format (--mount-option):
  name=<name>[,dst=<target-path>][,subpath=<sub-path>][,readonly]

Auth mode (--auth-mode):
  DEFAULT  Use backend default (currently TOKEN)
  TOKEN    All ports require X-Access-Token
  PUBLIC   envd management port (49983) requires token; other ports open
  NONE     No authentication on any port

Examples:
  ags instance create -t code-interpreter-v1
  ags instance create --tool-name code-interpreter-v1
  ags instance create --tool-id sdt-xxxx
  ags instance create -t my-tool --timeout 600
  ags instance create --tool-id sdt-xxxx --mount-option "name=data,dst=/workspace,subpath=user-123"
  ags instance create -t my-tool --auth-mode NONE

```
ags instance create [flags]
```

### Options

```
      --auth-mode string           Auth mode: DEFAULT, TOKEN, NONE, PUBLIC (default "DEFAULT")
      --client-token string        Client token for duplicate creation protection
  -h, --help                       help for create
      --mount-option stringArray   Mount option to override tool storage config
                                   Mount option to override tool storage configuration.
                                   
                                   Format:
                                     name=<name>[,dst=<target-path>][,subpath=<sub-path>][,readonly]
                                   
                                   Parameters:
                                     name      Storage mount name defined in tool (required)
                                     dst       Override target mount path in container (optional)
                                     subpath   Sub-directory isolation path (optional)
                                     readonly  Force read-only mount (optional, can only tighten permissions)
                                   
                                   Examples:
                                     --mount-option "name=data,dst=/workspace,subpath=user-123"
                                     --mount-option "name=models,readonly"
      --request string             Complete request body as JSON, @file, or - for stdin
      --timeout int                Instance timeout in seconds (default 300)
      --tool-id string             Tool ID (cloud backend only)
  -t, --tool-name string           Tool name (e2b/cloud backend)
```

### Options inherited from parent commands

```
      --api-key string      API key
      --backend string      API backend: e2b or cloud
      --config string       config file (default is $HOME/.ags/config.toml)
      --domain string       Base domain (default: tencentags.com)
      --internal            Use internal endpoints
      --jq string           jq expression (only with -o json)
      --no-color            Disable ANSI color output
      --non-interactive     Disable interactive behaviors
  -o, --output string       output format: text, json, or ndjson
      --region string       Region for API access (default: ap-guangzhou)
      --secret-id string    Tencent Cloud SecretID
      --secret-key string   Tencent Cloud SecretKey
```

### SEE ALSO

* [ags instance](ags_instance.md)	 - Manage sandbox instances

###### Auto generated by spf13/cobra on 14-May-2026
